state = { }  x  => main 
binding using es6  x  => main
functional stateless components => card
expression to check && component => main
check for null prop and show loading => main
fragment to wrap array of items not in div => main => search controls
context api => main + card
pass props down using spread => <MyComponent amount={this.state.tet} {...this.props} >  => main renderContentCards

new refs
