state = { }
binding using es6
functional stateless components
expression to check && component
check for null prop and show loading
template to wrap array of items not in div
context api
spread state
console.log({ item, othergintg })
const { id, ...rest} = item;
[...HTMLElementList].foreach or [].call or ArrayFrom
~ for indexOf

pass props down using spread => <MyComponent amount={this.state.tet} {...this.props} >
React portal pass to parent node
new refs
